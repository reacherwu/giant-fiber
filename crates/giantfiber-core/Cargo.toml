[package]
name = "giantfiber-core"
version = "0.1.0"
edition = "2021"
authors = ["GiantFiber Team <research@giantfiber.ai>"]
description = "High-performance Sub-5ms Drosophila Connectome Bio-Reflex Coprocessor Core"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# Zero external dependencies for maximum portability, zero-bloat, and sub-millisecond execution

[features]
default = ["std"]
std = []

[dev-dependencies]
